let registerForm=document.getElementById("registerForm");
if(registerForm){registerForm.addEventListener("submit",function(event){event.preventDefault();let name=document.getElementById("name").value.trim();let email=document.getElementById("email").value.trim();let college=document.getElementById("college").value.trim();let course=document.getElementById("course").value.trim();let skills=document.getElementById("skills").value.trim();let password=document.getElementById("password").value;let profiles=JSON.parse(localStorage.getItem("profiles"))||[];let existingUser=profiles.find(function(profile){return profile.email===email;});if(existingUser){alert("An account with this email already exists.");return;}let newProfile={id:Date.now(),name:name,email:email,college:college,course:course,skills:skills,password:password};profiles.push(newProfile);localStorage.setItem("profiles",JSON.stringify(profiles));alert("Your profile has been created successfully!");window.location.href="login.html";});}
let loginForm=document.getElementById("loginForm");
if(loginForm){loginForm.addEventListener("submit",function(event){event.preventDefault();let email=document.getElementById("loginEmail").value.trim();let password=document.getElementById("loginPassword").value;let profiles=JSON.parse(localStorage.getItem("profiles"))||[];let user=profiles.find(function(profile){return profile.email===email&&profile.password===password;});if(user){localStorage.setItem("loggedInUser",JSON.stringify(user));alert("Login successful!");window.location.href="profiles.html";}else{alert("Invalid email or password.");}});}
